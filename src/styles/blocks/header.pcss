.header {
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  &__menu {
    @include tablets {
      display: none;
    }
  }
  &__socials {
    @include tablets {
      padding-left: 30%;
    }
    @include phones {
      display: none;
    }
  }
}
